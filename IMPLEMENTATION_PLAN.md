# Python·Git 퀴즈 게임 구현 계획

이 문서는 `Ques.md`의 필수 요구사항과 보너스 5종을 모두 구현하기 위한 설계 기준과 작업 순서를 정리합니다. 구현이 끝났는지는 파일 존재 여부가 아니라 자동 테스트, 실제 콘솔 실행, 재실행 후 데이터 유지, Git 이력으로 판단합니다.

> 이 파일은 구현 전 작성한 초기 계획입니다. 실제 결과물은 학습하기 쉬운 최소 구조를 위해 `main.py`, `classes/quiz.py`, `classes/quizgame.py`로 단순화했습니다. 최종 실행 방법과 데이터 구조는 `README.md`를 기준으로 확인합니다.

## 1. 목표와 구현 범위

Python 3.10 이상과 표준 라이브러리만 사용해 사지선다형 콘솔 퀴즈 게임을 만듭니다. 퀴즈 주제는 기존 `README.md`의 방향을 따라 **Python과 Git 기초**로 정합니다.

### 필수 범위

- 메뉴 출력과 안전한 종료
- 퀴즈 풀기, 추가, 목록 조회, 최고 점수 조회
- `Quiz`, `QuizGame` 클래스 분리
- 직접 작성한 기본 퀴즈 5개 이상 제공
- 프로젝트 루트의 `state.json`에 퀴즈와 최고 점수 저장
- 빈 입력, 문자 입력, 범위 밖 숫자, `Ctrl+C`, EOF 처리
- 상태 파일 없음, 손상, 읽기·쓰기 실패 처리
- README 필수 항목과 제출 증거 준비
- 10개 이상의 의미 있는 커밋, 기능 브랜치 병합, `clone`과 `pull` 실습

### 보너스 범위

- 문제 순서 무작위 섞기
- 풀 문제 수 선택
- 힌트 표시와 점수 차감
- 퀴즈 삭제와 즉시 저장
- 날짜·문제 수·점수를 포함한 전체 게임 기록 저장

필수 기능을 모두 검증한 후 보너스를 완료하는 순서로 진행합니다. 보너스 구현으로 필수 요구사항의 동작이 약해지지 않도록 각 단계를 테스트합니다.

## 2. 현재 상태

현재 폴더에는 `Ques.md`와 `README.md`만 있습니다. `README.md`에는 `main.py`, `src/`, `tests/`, `docs/`와 검증 결과가 이미 있는 것처럼 적혀 있지만 실제 파일은 없습니다. 구현 단계에서는 계획에 맞춰 파일을 만든 후 실제 결과를 기준으로 README를 다시 맞춰야 합니다.

또한 `piscine/01`은 상위 Git 저장소 안에 있습니다. 과제에서 독립된 GitHub 저장소 URL을 요구하므로 Git 실습을 시작하기 전에 다음 원칙을 적용합니다.

- `piscine/01`을 독립 저장소로 사용할지 먼저 확정합니다.
- 독립 저장소로 사용한다면 모든 Git 명령은 `piscine/01`에서 실행합니다.
- 상위 저장소에 중첩 저장소를 실수로 추가하지 않습니다.
- 원격 저장소 생성, push, 공개 범위 변경은 사용자 확인 후 진행합니다.

## 3. 완성 후 파일 구조

```text
01/
├── .gitignore
├── Ques.md
├── README.md
├── IMPLEMENTATION_PLAN.md
├── main.py
├── state.json
├── src/
│   ├── __init__.py
│   └── quiz_game.py
├── tests/
│   ├── test_quiz.py
│   ├── test_quiz_game.py
│   └── test_console.py
├── scripts/
│   └── verify.sh
└── docs/
    └── screenshots/
        └── README.md
```

- `main.py`: 실행 진입점입니다. 객체를 만들고 게임을 시작합니다.
- `src/quiz_game.py`: `Quiz`, `QuizGame`, 상태 파일 입출력 로직을 둡니다.
- `state.json`: 퀴즈, 최고 기록, 전체 게임 기록을 UTF-8 JSON으로 저장합니다.
- `tests/`: 모델, 저장, 콘솔 입출력, 예외 상황을 검증합니다.
- `scripts/verify.sh`: Python 버전, 문법 검사, 단위 테스트, 핵심 콘솔 시나리오를 한 번에 실행합니다.
- `docs/screenshots/README.md`: 제출에 필요한 스크린샷 목록과 촬영 조건을 기록합니다.

## 4. 클래스와 책임

### `Quiz`

퀴즈 한 문제의 데이터와 판정 규칙만 담당합니다.

```python
class Quiz:
    question: str
    choices: list[str]
    answer: int
    hint: str

    def display(self, number: int) -> None: ...
    def is_correct(self, selected: int) -> bool: ...
    def to_dict(self) -> dict: ...

    @classmethod
    def from_dict(cls, data: dict) -> "Quiz": ...
```

생성할 때 다음 규칙을 검사합니다.

- 문제는 공백을 제거한 뒤 비어 있지 않아야 합니다.
- 선택지는 정확히 4개이며 각 항목이 비어 있지 않아야 합니다.
- 정답은 `1`부터 `4`까지의 정수여야 합니다.
- 힌트는 보너스 기능에 사용하므로 기본 퀴즈와 새 퀴즈 모두 저장합니다.

### `QuizGame`

메뉴, 입력, 게임 흐름, 점수, 상태 저장을 담당합니다.

```python
class QuizGame:
    quizzes: list[Quiz]
    best_record: dict | None
    history: list[dict]

    def run(self) -> None: ...
    def play_quiz(self) -> None: ...
    def add_quiz(self) -> None: ...
    def list_quizzes(self) -> None: ...
    def delete_quiz(self) -> None: ...
    def show_best_score(self) -> None: ...
    def show_history(self) -> None: ...
    def load_state(self) -> None: ...
    def save_state(self) -> bool: ...
```

입력 함수는 한곳에 모읍니다. 숫자 입력은 `strip()`을 적용하고 빈 값, 변환 실패, 범위 오류를 구분해 안내한 뒤 같은 입력으로 돌아갑니다. `KeyboardInterrupt`와 `EOFError`는 공통 종료 신호로 바꾸고, 최상위 게임 루프가 저장을 시도한 뒤 정상 종료합니다.

## 5. 메뉴와 주요 흐름

보너스 기능을 별도 메뉴로 드러내기 위해 다음 구성을 사용합니다.

```text
1. 퀴즈 풀기
2. 퀴즈 추가
3. 퀴즈 목록
4. 퀴즈 삭제
5. 최고 점수 확인
6. 점수 기록 확인
7. 종료
```

### 퀴즈 풀기

1. 저장된 퀴즈가 없으면 안내하고 메뉴로 돌아갑니다.
2. 사용자가 `1`부터 전체 퀴즈 수 사이에서 풀 문제 수를 고릅니다.
3. 원본 목록을 바꾸지 않도록 복사한 뒤 `random.sample()`로 문제를 뽑고 순서를 섞습니다.
4. 각 문제에서 `1`~`4` 또는 `h`를 입력받습니다.
5. `h`를 처음 입력하면 힌트를 보여 주고 해당 문제에 힌트 사용을 기록합니다. 같은 문제에서 다시 `h`를 입력하면 이미 사용했다는 메시지를 출력합니다.
6. 정답을 제출하면 정답·오답을 알려 줍니다.
7. 모든 문제를 푼 뒤 점수, 정답 수, 힌트 사용 수를 표시합니다.
8. 게임 기록을 추가하고 최고 기록을 갱신한 뒤 즉시 저장합니다.

점수는 다음처럼 계산합니다.

```text
기본 점수 = round(정답 수 / 푼 문제 수 * 100)
최종 점수 = max(0, 기본 점수 - 힌트 사용 수 * 10)
```

최고 기록은 최종 점수가 높은 게임을 우선합니다. 점수가 같으면 더 많은 문제를 푼 기록, 힌트를 적게 쓴 기록, 더 최근 기록 순서로 비교합니다. 이 규칙은 문제 수 선택 기능을 사용해도 기록 비교가 일관되게 만듭니다.

### 퀴즈 추가

1. 문제를 입력받고 빈 값이면 다시 입력받습니다.
2. 서로 다른 선택지 4개를 입력받습니다.
3. 정답 번호 `1`~`4`를 입력받습니다.
4. 빈 값이 아닌 힌트를 입력받습니다.
5. `Quiz` 객체를 만든 뒤 목록에 추가합니다.
6. 저장에 성공해야 완료 메시지를 출력합니다.

저장에 실패하면 메모리에 추가한 퀴즈를 되돌리고 오류를 알립니다. 화면에는 성공으로 표시됐지만 재실행 후 사라지는 상태를 만들지 않습니다.

### 퀴즈 목록과 삭제

- 목록에서는 번호와 문제를 표시합니다. 필요하면 정답과 힌트는 숨겨 평가 중 답이 노출되지 않게 합니다.
- 삭제에서는 먼저 번호와 문제를 보여 주고 삭제 번호를 검증합니다.
- 삭제 전 `y/n` 확인을 받습니다.
- 삭제 후 즉시 저장합니다. 저장 실패 시 삭제한 객체를 원래 위치에 복구합니다.
- 마지막 퀴즈도 삭제할 수 있습니다. 빈 목록은 유효한 사용자 상태이며 프로그램이 임의로 기본 퀴즈를 되살리지 않습니다.

### 점수 조회

- 플레이 기록이 없으면 아직 퀴즈를 풀지 않았다고 안내합니다.
- 최고 점수 화면에는 점수, 정답 수, 문제 수, 힌트 사용 수, 날짜를 표시합니다.
- 전체 기록 화면에는 최신 기록부터 번호를 붙여 표시합니다.

## 6. `state.json` 설계

```json
{
  "schema_version": 1,
  "quizzes": [
    {
      "question": "Python에서 리스트를 만드는 기호는?",
      "choices": ["()", "[]", "{}", "<>"],
      "answer": 2,
      "hint": "대괄호를 사용합니다."
    }
  ],
  "best_record": {
    "played_at": "2026-08-04T15:30:00+09:00",
    "total": 5,
    "correct": 4,
    "hints_used": 1,
    "score": 70
  },
  "history": [
    {
      "played_at": "2026-08-04T15:30:00+09:00",
      "total": 5,
      "correct": 4,
      "hints_used": 1,
      "score": 70
    }
  ]
}
```

- 날짜는 `datetime.now().astimezone().isoformat(timespec="seconds")`로 기록합니다.
- JSON은 `ensure_ascii=False`, `indent=2`, `encoding="utf-8"`로 읽고 씁니다.
- 저장할 때 같은 폴더의 임시 파일에 먼저 쓴 후 `os.replace()`로 교체합니다.
- 파일이 없으면 기본 퀴즈 5개, 최고 기록 없음, 빈 히스토리로 시작한 뒤 상태 파일을 만듭니다.
- JSON 문법이나 스키마가 손상되면 원본을 `state.json.corrupt-날짜시간`으로 보존하고 기본 상태로 복구합니다.
- 정상적으로 저장된 빈 `quizzes` 배열은 손상으로 판단하지 않습니다.
- 기존 필드가 없는 이전 상태 파일을 고려해 `schema_version`과 기본값을 사용합니다.

## 7. 예외 처리 기준

| 상황 | 기대 동작 |
|---|---|
| 숫자 앞뒤에 공백 입력 | 공백을 제거하고 정상 처리 |
| 빈 입력 | 이유를 안내하고 같은 단계 재입력 |
| `abc` 입력 | 숫자 또는 허용 명령을 안내하고 재입력 |
| 허용 범위 밖 숫자 | 현재 허용 범위를 보여 주고 재입력 |
| 메뉴에서 `Ctrl+C` 또는 EOF | 저장을 시도하고 종료 코드 `0`으로 종료 |
| 플레이 중 `Ctrl+C` 또는 EOF | 미완료 게임은 기록하지 않고 기존 상태 저장 후 종료 |
| `state.json` 없음 | 기본 퀴즈로 시작하고 새 파일 생성 |
| JSON 문법 손상 | 손상 파일 백업, 안내, 기본 상태 복구 |
| JSON 필드·타입 손상 | 손상 파일 백업, 안내, 기본 상태 복구 |
| 저장 권한 또는 디스크 오류 | 실패를 알리고 성공으로 표시하지 않음 |
| 퀴즈가 없음 | 플레이·목록·삭제에서 안내 후 메뉴 복귀 |

## 8. 구현 순서

### 1단계: 실행 골격

- `.gitignore`, `main.py`, `src/__init__.py`, `src/quiz_game.py`를 만듭니다.
- Python 3.10 이상인지 확인하는 실행 안내를 작성합니다.
- 메뉴 반복과 정상 종료부터 구현합니다.

완료 기준: `python3 main.py`로 메뉴가 반복되고 잘못된 메뉴 입력 후에도 종료되지 않습니다.

### 2단계: 모델과 기본 데이터

- `Quiz` 클래스와 유효성 검사를 구현합니다.
- Python·Git 주제 기본 문제를 5개 이상 만듭니다.
- `to_dict()`, `from_dict()` 변환을 구현합니다.

완료 기준: 유효한 퀴즈는 왕복 변환되고 선택지 수나 정답 범위가 잘못되면 거부됩니다.

### 3단계: 파일 영속성

- 상태 스키마 검증, 불러오기, 원자적 저장을 구현합니다.
- 파일 없음, 손상 파일, 읽기·쓰기 오류를 처리합니다.

완료 기준: 퀴즈를 추가하거나 기록을 만든 뒤 재실행해도 데이터가 유지됩니다.

### 4단계: 필수 메뉴 기능

- 퀴즈 풀기, 추가, 목록, 최고 점수를 구현합니다.
- 퀴즈 없음과 아직 기록 없음 상태를 처리합니다.
- 모든 숫자 입력에 공통 검증 함수를 적용합니다.

완료 기준: Ques.md의 필수 콘솔 시나리오를 처음부터 끝까지 실행할 수 있습니다.

### 5단계: 보너스 기능

- `feature/quiz-play-bonus` 브랜치에서 무작위 출제, 문제 수 선택, 힌트를 구현합니다.
- 브랜치를 `main`에 병합합니다.
- 퀴즈 삭제와 기록 히스토리를 추가합니다.

완료 기준: 5개 보너스가 상태 저장과 함께 동작하고 기존 필수 기능 테스트가 모두 통과합니다.

### 6단계: 자동 검증

- 클래스 단위 테스트를 작성합니다.
- 임시 디렉터리의 독립된 상태 파일로 저장·복구를 검증합니다.
- `subprocess`로 실제 콘솔 입력과 종료를 검증합니다.
- `scripts/verify.sh`에서 전체 검증을 한 번에 실행합니다.

완료 기준: 깨끗한 환경에서 외부 패키지 없이 검증 스크립트가 성공합니다.

### 7단계: 문서와 제출 증거

- 실제 구현에 맞춰 README를 고칩니다.
- 실행 방법, 기능, 구조, 데이터 스키마를 기록합니다.
- 수동 캡처 목록에 따라 스크린샷을 준비합니다.
- 원격 GitHub 저장소에서 `clone`과 `pull` 실습을 완료합니다.

완료 기준: README의 명령과 파일 경로가 실제 프로젝트와 일치하고 모든 제출 증거를 찾을 수 있습니다.

## 9. 테스트 계획

### 단위 테스트

- `Quiz` 생성, 정답 판정, 직렬화·역직렬화
- 선택지 개수, 빈 문자열, 정답 범위 검증
- 점수와 힌트 차감 계산
- 최고 기록 비교와 동점 처리
- 퀴즈 추가·삭제 시 목록 변경
- 무작위 추출 결과의 개수와 중복 여부

### 상태 파일 테스트

- 파일이 없는 첫 실행에서 기본 퀴즈 로드
- UTF-8 한글 퀴즈 저장과 재로드
- 최고 기록과 전체 히스토리 유지
- 문법이 깨진 JSON 복구와 백업 생성
- 필드 타입이나 퀴즈 스키마가 잘못된 JSON 복구
- 유효한 빈 퀴즈 목록 유지
- 저장 실패 시 추가·삭제 롤백

### 콘솔 통합 테스트

- 메뉴의 빈 값, 문자, `0`, 범위 밖 숫자
- 정답 입력의 공백, 문자, `0`, `5`
- 퀴즈 추가의 빈 문제·선택지와 잘못된 정답 번호
- 문제 수 선택의 잘못된 입력
- 힌트 1회 사용과 중복 요청
- 퀴즈가 없을 때 플레이·목록·삭제
- 기록이 없을 때 최고 점수·히스토리 조회
- 정상 종료, EOF 종료, 가능한 경우 `Ctrl+C` 종료
- 종료 후 재실행했을 때 퀴즈와 기록 유지

무작위 동작은 테스트에서 난수 발생기를 주입하거나 `random.sample()`을 모킹해 결과를 재현할 수 있게 만듭니다. 실제 실행 검증에서는 순서가 바뀌어도 문제 수와 중복 없음만 확인합니다.

## 10. 요구사항 추적표

| ID | 요구사항 | 구현 위치 | 검증 방법 |
|---|---|---|---|
| R1 | 메뉴, 종료, 잘못된 입력 | `QuizGame.run`, 입력 헬퍼 | 콘솔 통합 테스트 |
| R2 | `Quiz`, `QuizGame` 2개 클래스 | `src/quiz_game.py` | 정적 검사, 단위 테스트 |
| R3 | 기본 퀴즈 5개 이상 | 기본 데이터 함수 | 개수·스키마 단위 테스트 |
| R4 | 퀴즈 풀기와 결과 | `play_quiz` | 전체 플레이 통합 테스트 |
| R5 | 퀴즈 추가와 저장 | `add_quiz`, `save_state` | 추가 후 재실행 테스트 |
| R6 | 퀴즈 목록과 빈 상태 | `list_quizzes` | 콘솔 통합 테스트 |
| R7 | 최고 점수 갱신·저장 | 기록 계산 함수 | 비교·재실행 테스트 |
| R8 | UTF-8 `state.json` | 상태 저장소 로직 | 한글 저장 왕복 테스트 |
| R9 | 파일 없음·손상·입출력 오류 | `load_state`, `save_state` | 임시 파일 오류 테스트 |
| R10 | `Ctrl+C`, EOF 안전 종료 | 최상위 실행 루프 | subprocess 종료 테스트 |
| B1 | 무작위 출제 | `play_quiz` | 중복·개수·원본 보존 테스트 |
| B2 | 문제 수 선택 | `play_quiz` | 경계값 통합 테스트 |
| B3 | 힌트와 점수 차감 | `Quiz.hint`, 점수 함수 | 힌트별 점수 단위 테스트 |
| B4 | 퀴즈 삭제와 저장 | `delete_quiz` | 삭제 후 재실행 테스트 |
| B5 | 전체 게임 기록 | `history`, `show_history` | 다회 플레이 저장 테스트 |
| G1 | 10개 이상 의미 있는 커밋 | Git 이력 | `git log --oneline --graph --all` |
| G2 | 브랜치 생성과 병합 | `feature/quiz-play-bonus` | Git 그래프 확인 |
| G3 | `clone`, `pull` 실습 | 별도 복제 디렉터리 | 명령 기록과 반영 결과 캡처 |
| D1 | README 필수 항목 | `README.md` | 항목·명령·경로 수동 검사 |
| D2 | 제출 스크린샷 | `docs/screenshots/` | 파일 존재와 민감정보 확인 |

## 11. Git 작업 계획

다음은 10개를 넘기기 위한 빈 커밋 쪼개기가 아니라, 각 단계가 독립적으로 설명되고 되돌릴 수 있게 나눈 예시입니다.

1. `Chore: 프로젝트 실행 구조와 gitignore 추가`
2. `Feat: Quiz 모델과 유효성 검사 구현`
3. `Feat: Python Git 기본 퀴즈 데이터 추가`
4. `Feat: 메뉴와 공통 입력 예외 처리 구현`
5. `Feat: JSON 상태 저장과 복구 구현`
6. `Feat: 퀴즈 추가와 목록 조회 구현`
7. `Feat: 퀴즈 출제와 점수 계산 구현`
8. `Feat: 랜덤 출제 문제 수 선택 힌트 구현`
9. `Merge: feature/quiz-play-bonus 브랜치 병합`
10. `Feat: 퀴즈 삭제와 저장 롤백 구현`
11. `Feat: 최고 기록과 게임 히스토리 구현`
12. `Test: 모델 저장 콘솔 테스트 추가`
13. `Docs: 실행 방법과 상태 스키마 정리`

병합 기록을 분명히 남기려면 기능 브랜치에서 작업한 뒤 `git merge --no-ff feature/quiz-play-bonus`를 사용합니다. 커밋 수는 병합 후 `git rev-list --count HEAD`로, 그래프는 `git log --oneline --graph --decorate --all`로 확인합니다.

개발 완료 후 `clone`과 `pull`은 다음 순서로 실습합니다.

1. 원격 저장소를 별도 디렉터리에 `git clone`합니다.
2. 복제본의 README에 실습 문장을 추가합니다.
3. 복제본에서 commit과 push를 수행합니다.
4. 원래 작업 폴더에서 `git pull`을 실행합니다.
5. README 변경과 Git 그래프를 확인합니다.

## 12. 제출 증거 계획

다음 자료는 실제 실행 후 직접 생성합니다. 계획 단계에서는 완료로 표시하지 않습니다.

- GitHub 저장소 URL
- `python3 --version`과 `git --version`이 보이는 개발 환경 화면
- 메인 메뉴 화면
- 퀴즈 추가 성공 화면
- 추가한 퀴즈가 보이는 목록 화면
- 문제 수 선택, 무작위 출제, 힌트 사용 화면
- 퀴즈 완료 결과와 최고 점수 화면
- 퀴즈 삭제와 재실행 후 반영 화면
- 여러 항목이 있는 점수 히스토리 화면
- `git log --oneline --graph --decorate --all` 화면
- `clone` 후 변경을 기존 폴더에서 `pull`한 결과

캡처 전에 Git 사용자 이메일, 원격 URL의 토큰, 로컬의 개인 경로 등 민감정보가 노출되지 않았는지 확인합니다.

## 13. 최종 완료 기준

다음 조건을 모두 만족해야 구현 완료로 판단합니다.

- Python 3.10 이상에서 외부 패키지 없이 실행됩니다.
- 필수 기능 4종과 종료 기능이 실제 콘솔에서 동작합니다.
- 보너스 기능 5종이 모두 동작합니다.
- 기본 퀴즈가 5개 이상이며 모든 문제에 선택지 4개와 힌트가 있습니다.
- 퀴즈, 최고 기록, 전체 기록이 재실행 후에도 유지됩니다.
- 모든 숫자 입력 오류, `Ctrl+C`, EOF, 상태 파일 손상을 안전하게 처리합니다.
- 자동 테스트와 `scripts/verify.sh`가 통과합니다.
- README가 실제 파일 구조와 실행 결과를 설명합니다.
- 의미 있는 커밋 10개 이상과 브랜치 병합 기록이 있습니다.
- `init`, `add`, `commit`, `push`, `pull`, `checkout`, `clone` 사용 근거가 있습니다.
- 제출 스크린샷과 GitHub 저장소 URL이 준비되어 있습니다.
